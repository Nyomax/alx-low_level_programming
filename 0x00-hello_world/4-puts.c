#include <stdio.h>
/**
 * includes brings the standard input out library for c
 * Returns :o when we specific int and writes a text
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}	
