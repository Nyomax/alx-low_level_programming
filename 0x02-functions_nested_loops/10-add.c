#include "main.h"
/**
 *add - function that adds two integers and returns the result
 *Description:adds two integer value
 *@a:first argument
 *@b:second argument
 *Return:addition result
 */
int add(int a, int b)
{
	return (a + b);
}
